apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dell-csi-drivers.fullname" . }}-test-connection"
  labels:
    {{- include "dell-csi-drivers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dell-csi-drivers.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
